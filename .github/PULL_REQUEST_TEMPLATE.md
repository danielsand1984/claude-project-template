## Summary

<!-- 1-3 sentences. What changed and why. Link any related Beads issue (e.g. closes webapp-test-abc). -->

## Test plan

<!-- Bulleted checklist of how you verified this works. Be specific. -->

- [ ] Lint passes (`npm run lint` / `ruff check .`)
- [ ] Tests pass (`npm test` / `pytest -q`)
- [ ] Manually verified the happy path
- [ ] Manually verified the obvious edge cases
- [ ] (if UI) screenshots / recording attached
- [ ] (if DB) migration runs forward and is idempotent

## Risks

<!-- What could break? What's the rollback plan? Delete if low-risk. -->

## Docs touched

<!-- Did you update CLAUDE.md, INDEX.md, docs/ai-instructions/*, ADRs? -->
