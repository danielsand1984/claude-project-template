export default function HomePage() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>{`{{PROJECT_TITLE}}`}</h1>
      <p>{`{{PROJECT_DESCRIPTION}}`}</p>
    </main>
  );
}
