# Keep this folder in git so the Dockerfile's `COPY /app/public ./public` line works.
# Drop your favicon.ico, robots.txt, og-image.png etc. here. Files in public/ are
# served at the site root: public/foo.png → https://yoursite.com/foo.png.
